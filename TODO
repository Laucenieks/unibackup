 * Add configurable possibility to make md5 hash files for backup types:
	* ftp
	* sftp
	* smbclient
 * Forced backup (e.g. before update).
 * Configurable: Remove only increments for X older backups.
